[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc = "0.8.24"
optimizer = true
optimizer_runs = 200

remappings = [
  "@openzeppelin/=lib/openzeppelin-contracts/",
]

[profile.default.fmt]
line_length = 120
tab_width = 4
bracket_spacing = false

[profile.ci]
fuzz = { runs = 1000 }
